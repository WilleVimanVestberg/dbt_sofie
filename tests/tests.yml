version: 2

models:
  - name: silver_dedu
    description: "kolla om ack_id är unik"
    columns:
      - name: ack_Id
        description: "Unik identifierare"
        tests:
          - unique      # testar att alla ack_id är unika
          - not_null    # (valfritt) testar att inga värden saknas
